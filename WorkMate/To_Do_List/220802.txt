발견한 문제점
1. join_user시 데이터가 넘어가지 않는 문제> (캐릭터)데이터는 넘어왔으나 ping timeout으로 id가 바뀌어서 못찾는 거 일수도
                                      > (게임베이스)데이터가 넘어갔는지도 체크 불가능
2. createroom으로 방을 만들고 게임을 시작할 때 방 인원이 1명이하면 데이터를 삭제하는 로직 추가 필요

오늘 한거
1. getRoomIndex() 생성 > roomcnt 전역변수 제거
2. CreateRoom() 생성 > 방 생성 조건 간략화
3. 전반적인 코드 수정

다음에 할 거
1. 오늘 한 거 테스트 주석달기
2. join_user 문제점 파악 (ping-timeout)
3. 메인페이지(최성우) 코드 적용 && 캐릭터(이근우) 코드 적용
4. 매칭 실패 시 해당 유저의 데이터를 삭제하는 로직 필요
5. 3번 메인페이지 적용 시 입장하기 로직 추가