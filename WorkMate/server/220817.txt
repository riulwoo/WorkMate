1. 오늘 한 거 테스트 및 디버깅 << 무조건 먼저 해야함

// 민성
- 게임 시작 전 랜덤 게임 스타트 로직 추가

// 근우
- 클라이언트 코드 서버 변경코드에 맞게 수정

2. room객체를 json객체로 만들어 지는지 체크 > 모두 무산
3. json객체 넘겨서 접근 가능한지 체크

경우의 수
매칭 실패 후 roomout -1은 안뜨지만                        // 해결?
매칭 하지 않고 roomout -1이 뜨기 떄문에 if으로 막을 수 있다. // 해결

roomout 
-1이라서
방을 제대로 찾을수 없고
매칭 실패 후 방은 찾지만
방의 데이터를 삭제한 후 방을 없애버린다

방안의 유저가 한명도 없을 때.

